// creates a variable to store the number of likes and sets it to 0 to start
var likeCount = 0;
// the function named increaseLikes is defined
function increaseLikes() {
    // increment the likeCount variable by 1 each time the function is called
    likeCount = likeCount + 1;
}
